stages:
  - Build
  - Test

build:
  stage: Build
  tags:
    - shell
  script:
    - docker build -t software_image -f ./Dockerfiles/software_dockerfile .
  allow_failure: true

unittest:
  stage: Test
  tags:
    - shell
  script:
    - docker build -t kicad_image -f ./Dockerfiles/kicad_dockerfile .
  allow_failure: true
